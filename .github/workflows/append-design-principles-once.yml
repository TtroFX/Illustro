name: Mark PUB-02 preview complete once

on:
  push:
    branches: [main]
    paths:
      - '.github/design-principles-trigger'

permissions:
  contents: write

jobs:
  mark-pub-02:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Mark PUB-02 preview complete
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          path = Path('IMPLEMENTATION_PROGRESS.md')
          text = path.read_text()
          before = 'PUB-02 ユーザー確認2用Preview公開:未完了'
          after = 'PUB-02 ユーザー確認2用Preview公開:完了'
          if before in text:
              if text.count(before) != 1:
                  raise SystemExit(f'expected one progress line: {before}')
              text = text.replace(before, after, 1)
              path.write_text(text)
          elif after not in text:
              raise SystemExit(f'progress line missing: {before}')
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add IMPLEMENTATION_PROGRESS.md
          if git diff --cached --quiet; then
            echo 'PUB-02 progress already complete.'
            exit 0
          fi
          git commit -m 'chore: mark PUB-02 preview complete'
          git push origin HEAD:main
