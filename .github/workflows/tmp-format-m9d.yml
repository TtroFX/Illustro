name: Temporary M9D formatter

on:
  push:
    branches: [feature/m9d-linked-object-external]

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: '22'
          package-manager-cache: false
      - run: npm install --global npm@11.19.1
      - run: npm ci --ignore-scripts
      - run: npm run format
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/app/linked-object-external-controller.ts tests/unit/linked-object-external-controller.test.ts
          if ! git diff --cached --quiet; then
            git commit -m "chore: format M9D changes"
            git push
          fi
