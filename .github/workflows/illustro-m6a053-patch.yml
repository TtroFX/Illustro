name: Illustro one-shot M6A-053 patch

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  implement:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Restore dependencies
        run: npm ci
      - name: Apply M6A-053 rotation-jitter patch
        run: python3 scripts/patch-m6a053-rotation-jitter.py
      - name: Format changed TypeScript and verifier
        run: >-
          npx biome format --write
          src/domain/brush-schema.ts
          src/gpu/baseline-brush.ts
          src/app/canonical-raster-brush.ts
          src/app/paint-session-controller.ts
          src/app/brush-preset-library.ts
          src/app/brush-preset-controller.ts
          tests/unit/brush-rotation-jitter.test.ts
          scripts/verify-m6a-brush.mjs
      - name: Typecheck
        run: npm run typecheck
      - name: M6A-053 rotation-jitter regression
        run: npx vitest run tests/unit/brush-rotation-jitter.test.ts
      - name: Existing angle and random regression
        run: >-
          npx vitest run
          tests/unit/brush-tip-angle.test.ts
          tests/unit/brush-tip-direction.test.ts
          tests/unit/brush-follow-rotation.test.ts
          tests/unit/brush-orientation-mapping.test.ts
          tests/unit/brush-size-jitter.test.ts
          tests/unit/brush-opacity-jitter.test.ts
          tests/unit/brush-random-dynamics.test.ts
          tests/unit/brush-forced-taper.test.ts
          tests/unit/brush-post-stroke-correction.test.ts
          tests/unit/brush-stroke-repetition.test.ts
      - name: M6A verification
        run: npm run verify:m6a
      - name: Production build
        run: npm run build
      - name: Commit verified implementation
        shell: bash
        run: |
          rm -f scripts/patch-m6a053-rotation-jitter.py .github/workflows/illustro-m6a053-patch.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'feat: implement M6A rotation jitter'
          git push origin HEAD:main
