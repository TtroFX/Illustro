# Test fixtures

Deterministic input data used by automated verification. Fixtures are versioned, reviewable inputs and must not be generated from mutable external services during a verification run.
