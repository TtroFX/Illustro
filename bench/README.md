# Benchmarks

The benchmark harness emits structured JSON so results can be attached to verification evidence. M0 establishes repeatable execution only; feature-specific acceptance thresholds are defined by the later performance gates.
